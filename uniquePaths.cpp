#include <stdio.h>
#include <iostream>
#include <algorithm>
#include <math.h>
#include <vector>
#include <queue>
#include <stack>
#include <map>

using namespace std;
class Solution
{
public:
    int uniquePaths(int m, int n)
    {
        vector<vector<int>> dp(m * n, vector<int>(2));
        for (int i = 0; i < m*n; i++)
        {
            dp[i][0]
        }
    }
};
int main()
{
    Solution solution;

    printf();
    system("pause");
    return 0;
}
